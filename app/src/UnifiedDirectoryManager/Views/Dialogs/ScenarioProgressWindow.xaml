<Window x:Class="UnifiedDirectoryManager.Views.Dialogs.ScenarioProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Running scenario" Height="520" Width="680"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontWeight="SemiBold" FontSize="14" TextWrapping="Wrap"
                   Text="{Binding ScenarioName, StringFormat=Running “{0}”}" />
        <TextBlock Grid.Row="1" Foreground="#666" Margin="0,4,0,8" Text="{Binding ProgressText}" />

        <Border Grid.Row="2" BorderBrush="#DDD" BorderThickness="1">
            <ScrollViewer x:Name="LogScroller" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Lines}" Margin="6">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="0,1"
                                       FontFamily="Consolas" FontSize="12" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <TextBlock Grid.Row="3" Margin="0,10,0,0" FontWeight="SemiBold" TextWrapping="Wrap"
                   Text="{Binding Summary}" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <TextBlock Text="Working…  please wait" Foreground="#888" VerticalAlignment="Center" Margin="0,0,12,0"
                       Visibility="{Binding IsRunning, Converter={StaticResource BoolToVis}}" />
            <Button Content="Cancel" MinWidth="90" Margin="0,0,8,0" Command="{Binding CancelCommand}"
                    IsEnabled="{Binding CanCancel}"
                    Visibility="{Binding IsRunning, Converter={StaticResource BoolToVis}}" />
            <Button Content="Close" IsDefault="True" MinWidth="90" Click="OnClose"
                    IsEnabled="{Binding IsDone}" />
        </StackPanel>
    </Grid>
</Window>
